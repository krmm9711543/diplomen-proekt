{
    "name": "smart-home-system",
    "version": "1.0.0",
    "description": "хардуерна интеграция с микроконтролери, като осигурява обмен на даннии управление",
    "private": true,
    "scripts": {
        "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "start": "npm run dev"
    },
    "devDependencies": {
        "@types/node": "^25.4.0",
        "concurrently": "^8.2.2",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "@prisma/client": "^6.19.3",
        "dotenv": "^17.3.1"
    }
}
